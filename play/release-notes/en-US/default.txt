md 1.4 — charts, drawn from the numbers.

• New ```plot fence: functions, parametric curves and points, with title, axis labels and legend.
• Ranges, grid, axes and a colour per series — sin(x) * exp(-abs(x)/5) is one line.
• Plain vector drawn by the app: no engine, no network, crisp in preview, print, PDF, HTML and EPUB.
• Save a plot on its own with Export Diagram as SVG.
• Every file now reopens in the view you left it in — Edit, Split or Preview.
