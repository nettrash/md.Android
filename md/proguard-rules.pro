# md (Android) — ProGuard / R8 rules.
#
# The app is pure Kotlin + Jetpack Compose with no reflection-based
# libraries, so the default optimized rules suffice. Compose ships its
# own consumer rules. Nothing extra to keep.
