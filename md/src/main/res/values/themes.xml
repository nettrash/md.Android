<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose owns colors at runtime; this XML theme just provides a
         Material3 base so the splash window before Compose mounts looks
         right and the system bars transition cleanly. -->
    <style name="Theme.Md" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
